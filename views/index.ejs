<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>URL Shrinker</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" />

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <script src="https://kit.fontawesome.com/a2d040f73c.js" crossorigin="anonymous"></script>

  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #f4f4f4, #d7e9f7);
      min-height: 100vh;
    }

    .card {
      border: none;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
      border-radius: 1rem;
    }

    h1 {
      font-weight: 600;
      color: #2c3e50;
    }

    .btn-success {
      background-color: #28a745;
      border: none;
    }

    .btn-success:hover {
      background-color: #218838;
    }

    table a {
      word-break: break-all;
    }

    footer {
      text-align: center;
      margin-top: 2rem;
      color: #6c757d;
    }
  </style>
</head>
<body>
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-10 col-lg-8">
        <div class="card p-4">
          <h1 class="text-center mb-4">
            <i class="fas fa-link"></i> URL Shrinker
          </h1>

          <form action="/shortUrls" method="POST" class="d-flex gap-2 mb-4">
            <input 
              type="url" 
              name="fullURL" 
              id="fullURL" 
              placeholder="Enter a long URL..." 
              class="form-control" 
              required 
            />
            <button class="btn btn-success" type="submit">Shrink</button>
          </form>

          <div class="table-responsive">
            <table class="table table-bordered table-striped align-middle">
              <thead class="table-dark text-center">
                <tr>
                  <th>Original URL</th>
                  <th>Shortened URL</th>
                  <th>Clicks</th>
                </tr>
              </thead>
              <tbody class="text-center">
                <% shortUrls.forEach(shortUrl => { %>
                  <tr>
                    <td>
                      <a href="<%= shortUrl.full %>" target="_blank">
                        <%= shortUrl.full %>
                      </a>
                    </td>
                    <td>
                      <a href="https://url-shortner-ie9v.onrender.com/<%= shortUrl.short %>" target="_blank">
                        <%= shortUrl.short %>
                      </a>
                    </td>
                    <td><%= shortUrl.clicks %></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>

          <footer>
            Made with ❤️ by Siddhi Kadam
          </footer>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
